{
  "name": "@plastic/site",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "stencil build --prerender",
    "dev": "stencil build --dev --watch --serve --docs",
    "dev.app": "yarn dev",
    "test": "jest",
    "clean": "find www .stencil -delete"
  },
  "dependencies": {
    "@ionic/core": "^4.0.0-beta.11",
    "@stencil/router": "^0.2.6",
    "ionicons": "^4.4.3"
  },
  "devDependencies": {
    "@stencil/core": "^0.12.4",
    "@types/jest": "^23.3.2",
    "jest": "^23.6.0"
  }
}
