{
  "name": "@plastic/core-docs",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "module": "dist/esm/index.js",
  "collection": "dist/collection/collection-manifest.json",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build --docs",
    "dev": "stencil build --dev --watch --serve --docs",
    "dev.app": "stencil build --dev --watch --docs",
    "test": "jest",
    "clean": "find dist -delete"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^23.3.2",
    "jest": "^23.6.0",
    "ttypescript": "^1.5.5"
  },
  "dependencies": {
    "ts-transformer-keys": "^0.3.3",
    "@plastic/core": "1.0.0",
    "@stencil/core": "^0.12.4"
  },
  "jest": {
    "preset": "ts-jest",
    "globals": {
      "ts-jest": {
        "compiler": "ttypescript"
      }
    }
  }
}
